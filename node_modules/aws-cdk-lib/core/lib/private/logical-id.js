"use strict";Object.defineProperty(exports,"__esModule",{value:!0}),exports.LogicalIDs=void 0;var errors_1=()=>{var tmp=require("../errors");return errors_1=()=>tmp,tmp};class LogicalIDs{renames={};reverse={};addRename(oldId,newId){if(oldId in this.renames)throw new(errors_1()).UnscopedValidationError("RenameAlreadyRegistered",`A rename has already been registered for '${oldId}'`);this.renames[oldId]=newId}applyRename(oldId){let newId=oldId;if(oldId in this.renames&&(newId=this.renames[oldId]),newId in this.reverse&&this.reverse[newId]!==oldId)throw new(errors_1()).UnscopedValidationError("ObjectsAssignedSameLogical",`Two objects have been assigned the same Logical ID: '${this.reverse[newId]}' and '${oldId}' are now both named '${newId}'.`);return this.reverse[newId]=oldId,validateLogicalId(newId),newId}assertAllRenamesApplied(){const keys=new Set;if(Object.keys(this.renames).forEach(keys.add.bind(keys)),Object.keys(this.reverse).map(newId=>{keys.delete(this.reverse[newId])}),keys.size!==0){const unusedRenames=Array.from(keys.values());throw new(errors_1()).UnscopedValidationError("FollowingLogicalWereAttempted",`The following Logical IDs were attempted to be renamed, but not found: ${unusedRenames.join(", ")}`)}}}exports.LogicalIDs=LogicalIDs;const VALID_LOGICALID_REGEX=/^[A-Za-z0-9]{1,255}$/;function validateLogicalId(logicalId){if(!VALID_LOGICALID_REGEX.test(logicalId))throw new(errors_1()).UnscopedValidationError("LogicalAdhereRegularExpression",`Logical ID must adhere to the regular expression: ${VALID_LOGICALID_REGEX.toString()}, got '${logicalId}'`)}
